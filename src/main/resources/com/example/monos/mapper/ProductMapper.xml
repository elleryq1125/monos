<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.monos.mapper.ProductMapper">
  <!-- SQL -->  
	  <select id="selectList" resultType="com.example.monos.domain.Product">
			SELECT 
				product_id
				,company_id
				,product_code
				,name
				,unit
				,active
				,created_at
				,updated_at
				,deleted_at
			FROM 
				products
			WHERE
				company_id = #{companyId}
				AND active = #{active}
				<if test="productCode != null and productCode != ''">
					AND product_code LIKE CONCAT('%', #{productCode}, '%')
				</if>
				<if test="name != null and name != ''">
					AND name LIKE CONCAT('%', #{name}, '%')
				</if>
			ORDER BY
				product_code;
		</select>
		
		<select id="selectById" resultType="com.example.monos.domain.Product">
			SELECT 
				product_id
				,company_id
				,product_code
				,name
				,unit
				,active
				,created_at
				,updated_at
				,deleted_at
			FROM 
				products
			WHERE
				product_id = #{productId}
				AND company_id = #{companyId}
		</select>
		
		<select id="selectActiveByKeyword" resultType="com.example.monos.dto.ProductSuggestDto">
			SELECT 
				product_id
				,product_code
				,name
			FROM 
				products
			WHERE
			    company_id = #{companyId}
				AND (product_code LIKE CONCAT('%', #{keyword}, '%') OR name LIKE CONCAT('%', #{keyword}, '%'))
			ORDER BY
				product_code
			LIMIT 10
		</select>
		
		<select id="existsByProductCode" resultType="boolean">
			SELECT
				COUNT(*) > 0
			FROM
				products
			WHERE
				product_code = #{productCode}
				AND company_id = #{companyId}
		</select>
		
		<select id="existsByProductId" resultType="boolean">
			SELECT
				COUNT(*) > 0
			FROM
				products
			WHERE
				product_id = #{productId}
				AND company_id = #{companyId}
		</select>
		
		<insert id="insert" parameterType="com.example.monos.domain.Product" useGeneratedKeys="true" keyProperty="productId">
			INSERT INTO products(
				company_id,
				product_code,
				name,
				unit,
				active
			)
			VALUES(
				#{companyId},
				#{productCode},
				#{name},
				#{unit},
				#{active}
			)
		</insert>
		
		<update id="update" parameterType="com.example.monos.domain.Product">
			UPDATE products 
			SET
				name = #{name},
				unit = #{unit},
				active = #{active},
				updated_at = now()
			WHERE
				product_id = #{productId}
				AND company_id = #{companyId}
		</update>
</mapper>